$(".new_comment textarea").val("")
$("ol#comment #error_explanation").remove();
$("ol#comment").prepend("<%= 
    if !flash[:success].nil? 
      escape_javascript(render 'shared/comment', comment: @comment).html_safe 
    else
      escape_javascript(render 'shared/error_messages', object: @comment).html_safe 
    end
  %>")
